% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/math-nearest-integer.R
\name{rray_ceiling}
\alias{rray_ceiling}
\alias{rray_floor}
\alias{rray_trunc}
\alias{rray_round}
\alias{rray_signif}
\title{Rounding functions}
\usage{
rray_ceiling(x)

rray_floor(x)

rray_trunc(x)

rray_round(x, digits = 0)

rray_signif(x, digits = 6)
}
\arguments{
\item{x}{A vector, matrix, array or rray.}
}
\description{
\itemize{
\item \code{rray_ceiling()} - Compute the smallest integer value not less than \code{x}.
\item \code{rray_floor()} - Compute the largest integer value not greater than \code{x}.
\item \code{rray_trunc()} - Compute the nearest integer value not greater
\emph{in magnitude} than \code{x}.
\item \code{rray_round()} - Round \code{x} to the specified number of decimal places.
Rounding is done \emph{towards the even digit}, see \code{\link[base:round]{base::round()}} for more
information.
\item \code{rray_signif()} - Round \code{x} to the specified number of significant digits.
}
}
\examples{
x <- matrix(c(2.22, 4.5, 5.5))

# Round up
rray_ceiling(x)

# Round down
rray_floor(x)

# Truncate towards 0
rray_trunc(x)
rray_trunc(-x)

# Round to the specified number of decimal
# places. Border values (on a `5`) are rounded
# towards even numbers
rray_round(x)
rray_round(-x)

rray_round(x, digits = 1)

# Two significant digits
rray_signif(x, 2)

}
\concept{nearest integer math functions}
